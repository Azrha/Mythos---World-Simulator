version: "3.9"
services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: mythos
      POSTGRES_PASSWORD: mythos
      POSTGRES_DB: mythos
    ports:
      - "5432:5432"
    volumes:
      - mythos_db:/var/lib/postgresql/data
volumes:
  mythos_db:
